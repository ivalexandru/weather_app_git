nodemon:
  "scripts": {
    "dev": "npx nodemon ./src/app.js -e js,hbs"
  },

  -e js, hbs pt a face reload si pt extensia hbs (handlebars templating)

    term: npm run dev

am scos din scripts:
    "dev": "npx nodemon ./src/app.js -e js,hbs"
sa vad daca de acolo e eroarea (heroku ! [remote rejected] main -> main (pre-receive hook declined))



    

